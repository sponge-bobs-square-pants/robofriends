import React from 'react';

const Search = () => {
	return(
		<div>
			<input className = "pa3 ba b--green bg-lightest-blue"placeholder = "enter the name" type="Search" />
		</div>
	);
}

export default Search;